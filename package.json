{
  "name": "marblecms",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "biome format --write apps packages",
    "format-lint:fix": "biome check apps packages --write",
    "format-lint:check": "biome ci apps packages",
    "web:dev": "turbo dev --filter=web",
    "cms:dev": "turbo dev --filter=cms",
    "api:dev": "turbo dev --filter=api",
    "docs:dev": "turbo dev --filter=docs",
    "db:generate": "pnpm --filter db run db:generate",
    "db:push": "pnpm --filter db run db:push",
    "db:studio": "pnpm --filter db run db:studio",
    "db:migrate": "pnpm --filter db run db:migrate",
    "db:deploy": "pnpm --filter db run db:deploy",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@marble/tsconfig": "workspace:*",
    "husky": "^9.1.7",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8",
    "turbo": "^2.5.4",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.0.2",
      "@types/react-dom": "19.0.2"
    }
  },
  "packageManager": "pnpm@10.13.1"
}
